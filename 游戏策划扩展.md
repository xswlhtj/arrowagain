# 《箭途》扩展功能游戏策划与开发文档

> 文档用途：在基础版已经完整、可运行、测试通过的前提下，将课程作业中的附加功能拆分成可以逐项开发、逐项测试、逐项提交的增量任务。  
> 对应要求：更多关卡/关卡选择、得分/计时/星级、提示、进度保存、随机可通关关卡、动画与音效、可执行文件。  
> 文档版本：V1.0  
> 基础版本基线：3 个固定关卡、每关 3 次失误、开始/游戏/结果界面、四方向阻挡判断、飞出/碰撞动画、17 项自动化测试通过。

## 1. 扩展原则

### 1.1 核心目标

扩展版不是重写游戏，而是在现有基础版之上增加可独立启用的功能。每完成一个阶段，游戏都必须仍然能够启动、游玩原来的三个关卡并通过原有测试。

### 1.2 不允许被扩展破坏的基础行为

以下内容属于回归保护线：

1. 原第 1～3 关的棋盘大小、箭头布局和参考解法不变；
2. 每关初始失误机会仍固定为 `MAX_MISTAKES = 3`；
3. 箭头只检查正前方同一行或同一列，后方和斜对角不阻挡；
4. 点击空格不扣失误，受阻箭头不消失，成功箭头在动画结束后消失；
5. 动画期间不能重复结算棋盘点击；
6. 开始、游戏、通关、失败、全部通关、重新开始和返回首页流程保持可用；
7. 当前 `1200 × 800` 原生分辨率、高 DPI 清晰度和可爱星空主题保持不变；
8. 原有 17 项自动化测试必须始终通过。

### 1.3 扩展范围与优先级

| 优先级 | 功能 | 玩家价值 | 实现风险 | 建议阶段 |
|---|---|---:|---:|---|
| P1 | 提示功能 | 高 | 低 | 第一批 |
| P1 | 计时、得分和星级 | 高 | 低 | 第一批 |
| P1 | 关卡选择与进度保存 | 高 | 中 | 第二批 |
| P2 | 更多固定关卡 | 中 | 中 | 第二批 |
| P2 | 随机生成可通关关卡 | 高 | 高 | 第三批 |
| P2 | 更流畅的动画、粒子与音效 | 中 | 中 | 第四批 |
| P2 | 打包为 Windows 可执行文件 | 高 | 中 | 最后交付 |

推荐至少完整实现 P1 功能后再开发 P2。随机生成有较高展示价值，但不能以牺牲基础规则稳定性为代价。

## 2. 当前工程基线与扩展落点

### 2.1 现有模块职责

| 文件 | 当前职责 | 扩展时的处理原则 |
|---|---|---|
| `main.py` | 程序入口 | 保持轻量，不堆放功能代码 |
| `game/models.py` | 枚举和数据模型 | 仅追加带默认值的扩展字段，避免破坏现有构造方式 |
| `game/rules.py` | 阻挡、点击结算、胜负判断 | 保持纯 Python；提示和随机关卡验证复用这里的规则 |
| `game/levels.py` | 原 3 关模板与参考解法 | 原数据不改；新增关卡放在独立集合或新文件中 |
| `game/animation.py` | 飞出和碰撞动画 | 增加缓动类型时保持现有接口兼容 |
| `game/ui.py` | 字体、按钮、箭头和面板绘制 | 增加星级、开关、关卡卡片等复用组件 |
| `game/app.py` | 主循环、输入、界面绘制和状态切换 | 只负责组合功能，不在这里实现提示、存档或随机算法 |
| `tests/` | 基础规则和流程回归测试 | 原测试保留，扩展测试新增文件 |

### 2.2 建议新增模块

```text
game/
├─ scoring.py          # 计时、分数和星级计算，纯 Python
├─ hints.py            # 安全箭头搜索和提示选择，纯 Python
├─ progress.py         # JSON 进度读写、版本迁移和容错
├─ extra_levels.py     # 新增固定关卡，不改原 levels.py 中的三关
├─ generator.py        # 带随机种子的可解关卡生成器
├─ effects.py          # 粒子、星级动画等视觉效果
├─ audio.py            # 音效加载、播放、静音和异常降级
└─ resources.py        # 开发环境与 PyInstaller 环境统一资源路径

assets/
├─ start_background.png
├─ ui_background.png
├─ app.ico             # 打包图标
└─ audio/
   ├─ click.wav
   ├─ fly.wav
   ├─ collide.wav
   ├─ clear.wav
   └─ fail.wav

tests/
├─ test_scoring.py
├─ test_hints.py
├─ test_progress.py
└─ test_generator.py
```

新增模块并非要求一次性全部创建。某项功能开始实现时再创建对应文件，避免产生空模块。

## 3. 扩展后的玩家流程

```text
                         ┌──────────────┐
                         │  START 首页  │
                         └──────┬───────┘
                ┌───────────────┼─────────────────┐
                ↓               ↓                 ↓
          [继续游戏]       [关卡选择]        [随机挑战]
                │               │                 │
                └───────────────┴────────┬────────┘
                                         ↓
                                  [PLAYING 游戏中]
                                  ┌───────┴───────┐
                                  │               │
                                提示           正常点击
                                  │               │
                                  └───────┬───────┘
                                            ↓
                                  通关 / 失败结果界面
                                  │  分数、时间、星级  │
                                  └─────────┬────────┘
                                            ↓
                                保存纪录并解锁下一关
```

新增 `ScreenState.LEVEL_SELECT`。随机难度选择可以作为关卡选择界面中的一个卡片区域，不必继续增加顶层状态。

## 4. 数据模型增量设计

### 4.1 关卡元数据

原 `Level` 字段不删除。可以在末尾追加带默认值的字段，使原三个关卡的构造代码仍然有效：

```python
@dataclass(frozen=True)
class Level:
    id: int
    name: str
    rows: int
    cols: int
    arrows: tuple[ArrowSpec, ...]
    solution_order: tuple[str, ...]
    par_seconds: int = 90
    difficulty: str = "普通"
    pack: str = "classic"
```

原三关建议标准时间：第 1 关 60 秒、第 2 关 90 秒、第 3 关 120 秒。该数值只参与评分，不影响能否通关。

### 4.2 会话增量字段

建议在 `GameSession` 末尾追加：

```python
elapsed_seconds: float = 0.0
successful_moves: int = 0
collision_count: int = 0
hints_used: int = 0
hint_arrow_id: str | None = None
```

规则：

- `start_level()` 统一重置以上字段；
- `elapsed_seconds` 只在游戏可操作且帮助弹窗未打开时增加；
- 不把 Pygame 对象放入 `GameSession`，保证纯规则测试仍不依赖图形库。

### 4.3 进度数据

```python
@dataclass
class LevelRecord:
    cleared: bool = False
    best_score: int = 0
    best_time_ms: int | None = None
    best_stars: int = 0

@dataclass
class ProgressData:
    version: int = 1
    records: dict[str, LevelRecord] = field(default_factory=dict)
    resume: dict | None = None
    sound_enabled: bool = True
```

固定关卡键使用 `classic:1`、`classic:2`；随机关卡用种子记录，不混入固定关卡解锁表。

## 5. 功能 A：计时、得分与星级评价

### 5.1 玩家规则

- 进入关卡后计时从 `0.0` 开始；
- 打开玩法说明、进入结果页和窗口失去焦点时暂停计时；
- 成功飞出一支箭头获得 100 分；
- 只有通关后才结算失误奖励和时间奖励；
- 使用提示会扣除少量分数，但不会影响通关；
- 失败局不更新最佳纪录。

### 5.2 分数公式

设：

- `N`：成功飞出的箭头数；
- `M`：通关时剩余失误机会；
- `T`：向下取整后的通关秒数；
- `P`：关卡标准时间；
- `H`：提示次数；

```text
基础分   = N × 100
失误奖励 = M × 150
时间奖励 = max(0, P - T) × 10
提示扣分 = H × 100
最终得分 = max(0, 基础分 + 失误奖励 + 时间奖励 - 提示扣分)
```

分数必须通过 `scoring.py` 的纯函数计算，界面不得复制公式。

### 5.3 星级规则

| 星级 | 条件 |
|---|---|
| 3 星 | 通关；无失误；未使用提示；用时不超过标准时间 |
| 2 星 | 通关；最多 1 次失误；最多 1 次提示；用时不超过标准时间的 1.5 倍 |
| 1 星 | 其余所有已通关情况 |
| 0 星 | 未通关或失败 |

### 5.4 界面改动

右侧信息面板增加“用时”和“当前分数”。结果卡片显示：

```text
★★☆
本关得分  2350
通关用时  01:18
最佳纪录  2480 / 01:05
```

星星使用 `pygame.draw.polygon` 绘制，不依赖字体符号。刷新最佳纪录时增加“新纪录”标签。

### 5.5 实现步骤

1. 新建 `game/scoring.py`，实现 `calculate_score()`、`calculate_stars()`、`format_time()`；
2. 给会话追加计时和统计字段；
3. 在 `GameApp.update(dt)` 中按暂停条件累计时间；
4. 在成功动画和碰撞动画完成时更新统计；
5. 扩展右侧面板与结果卡片；
6. 新建 `tests/test_scoring.py`，覆盖零秒、超时、扣分下限、1/2/3 星边界。

### 5.6 验收标准

- 计时误差在 60 秒运行中不超过 0.2 秒；
- 帮助界面停留期间计时不增长；
- 同一局相同操作得到相同分数；
- 分数永不为负；
- 原关卡通关判断与 3 次失误规则不变。

## 6. 功能 B：提示功能

### 6.1 玩家规则

点击“提示”或按 `H`：

1. 搜索当前所有可以安全飞出的箭头；
2. 选择其中对后续解锁最有帮助的一支；
3. 用金色脉冲光圈高亮 2.5 秒；
4. 不替玩家点击，不改变箭头和失误次数；
5. 每次新提示计入一次提示使用并扣 100 分。

提示正在显示时再次点击提示，只重置高亮时间，不重复计数。

### 6.2 算法

`hints.py` 复用 `rules.is_blocked()`：

```python
def available_arrow_ids(arrows, rows, cols) -> list[str]: ...

def choose_hint(arrows, rows, cols) -> str | None: ...
```

选择策略：对每支当前可飞箭头进行一次模拟移除，计算“移除后新增的可飞箭头数”；优先选择该增量最大的箭头，平局时按 `(row, col, id)` 排序，确保测试结果稳定。

该算法不会读取 `solution_order`。因此固定关卡、残局和随机关卡都可使用同一套提示逻辑。

### 6.3 界面与反馈

- 在右侧工具区增加“提示 H”按钮；
- 高亮箭头外绘制两层呼吸圆环和小型“可飞出”标签；
- 底部消息显示“提示：这支箭头的路径是空的”；
- 动画播放中按钮禁用；没有可飞箭头时显示“当前布局无可用箭头”，但不扣分。

### 6.4 验收标准

- 提示给出的箭头在当前状态下一定能飞出；
- 提示不修改棋盘和失误次数；
- 连续点击提示不会在同一次高亮期间重复扣分；
- 三个基础关卡的任意合法残局均不会发生崩溃。

## 7. 功能 C：关卡选择与更多固定关卡

### 7.1 保留基础流程

原三关定义为“经典关卡包”，保持原布局和顺序。首页“开始游戏”仍进入经典第 1 关或最近未完成的经典关卡，确保基础演示路径不变。

新增“关卡选择”按钮，进入独立界面：

```text
┌─────────────────────────────────────────────────────┐
│                 选择关卡                            │
│  [1 方向热身 ★★★] [2 多链汇合 ★★☆] [3 密集联锁 ☆☆☆]│
│  [4 交错航线 🔒 ] [5 环形拆解 🔒 ] [6 星轨迷阵 🔒 ]│
│                                                     │
│  随机挑战： [休闲] [标准] [困难]       [返回首页]   │
└─────────────────────────────────────────────────────┘
```

### 7.2 解锁规则

- 原第 1～3 关始终可选，保证课程基础功能随时可演示；
- 完成第 3 关后解锁扩展第 4 关；
- 第 4～6 关依次解锁；
- 已通关关卡可以重复挑战并刷新最佳纪录；
- 锁定卡片不响应进入，只显示解锁条件。

### 7.3 新增关卡指标

| 关卡 | 棋盘 | 箭头数目标 | 初始可飞数 | 最长依赖深度目标 | 特色 |
|---|---:|---:|---:|---:|---|
| 4 交错航线 | 8×8 | 24 | 5 | 10 | 横纵链交错 |
| 5 环形拆解 | 8×8 | 28 | 5 | 17 | 外围与中心互相解锁 |
| 6 星轨迷阵 | 8×8 | 32 | 8 | 16 | 高密度、多分支汇合 |

新增关卡仍须包含四个方向，并为每关保存一条经程序验证的参考解法。先用生成器产生候选，再固定种子、人工试玩、冻结为 `extra_levels.py` 中的不可变数据，不能在每次启动时重新随机。

### 7.4 关卡目录接口

不要直接把所有场景继续硬编码到 `app.py`。建议提供：

```python
BASE_LEVELS = LEVELS
EXTRA_LEVELS = (...)
ALL_FIXED_LEVELS = BASE_LEVELS + EXTRA_LEVELS

def get_level(level_key: str) -> Level: ...
def next_level_key(level_key: str) -> str | None: ...
```

`GameSession` 最终应保存稳定的 `level_key`，而不是仅依赖列表下标。迁移期间可暂时保留 `current_level_index`，待扩展流程测试完成后再替换。

### 7.5 验收标准

- 原三个关卡数据逐项不变；
- 关卡选择能显示锁定、已解锁、已通关和最佳星级；
- 点击锁定关卡不进入游戏；
- 新增关卡全部通过静态校验、参考解法验证和人工试玩；
- 8×8 棋盘仍完整显示在现有棋盘面板内。

## 8. 功能 D：保存游戏进度

### 8.1 保存内容

分为两类：

1. **长期进度**：关卡是否通关、最佳分数、最佳时间、最佳星级、音效开关；
2. **单局续玩**：当前关卡/随机种子、剩余箭头 ID、剩余失误、用时和统计数据。

长期进度在通关结算后自动保存。单局续玩在玩家点击“保存并返回”时保存；不在动画中间保存。

### 8.2 保存位置

Windows 默认：

```text
%LOCALAPPDATA%\ArrowEscape\progress.json
```

若 `LOCALAPPDATA` 不可用，则回退到：

```text
用户目录\.arrow_escape\progress.json
```

存档不能写进项目源码目录或打包后的临时资源目录。

### 8.3 JSON 示例

```json
{
  "version": 1,
  "records": {
    "classic:1": {
      "cleared": true,
      "best_score": 1850,
      "best_time_ms": 42150,
      "best_stars": 3
    }
  },
  "resume": {
    "level_key": "classic:2",
    "random_seed": null,
    "remaining_arrow_ids": ["A", "B", "D"],
    "remaining_mistakes": 2,
    "elapsed_ms": 38420,
    "successful_moves": 13,
    "collision_count": 1,
    "hints_used": 0
  },
  "settings": {
    "sound_enabled": true
  }
}
```

### 8.4 安全与容错

- 先写 `progress.json.tmp`，刷新并成功关闭后用 `Path.replace()` 原子替换正式文件；
- 加载时验证顶层类型、版本号、关卡键、箭头 ID 子集和数值范围；
- 文件不存在、JSON 损坏或版本不支持时返回默认进度，不阻止游戏启动；
- 损坏文件可以重命名为 `progress.broken.json`，不要静默覆盖原文件；
- 存档只保存数据，不使用 `pickle`，避免不安全反序列化；
- `remaining_mistakes` 加载后强制限制在 `0～3`。

### 8.5 继续游戏

- 首页只有在存在有效 `resume` 时启用“继续游戏”；
- 固定关卡根据模板重建箭头，再按 `remaining_arrow_ids` 过滤；
- 随机关卡根据保存的种子和难度重新生成，再按 ID 过滤；
- 所有恢复箭头状态强制设为 `IDLE`；
- 成功恢复后清空所有临时动画和提示状态。

### 8.6 验收标准

- 关闭并重新启动游戏后，关卡纪录和音效开关仍存在；
- 续玩时棋盘、失误、计时和统计与保存时一致；
- 损坏存档不会导致程序崩溃；
- 新纪录只覆盖更优值，较差成绩不覆盖最佳成绩；
## 9. 功能 E：随机生成可通关关卡

### 9.1 难度预设

| 难度 | 棋盘 | 箭头数 | 初始可飞目标 | 依赖深度目标 |
|---|---:|---:|---:|---:|
| 休闲 | 6×6 | 12～14 | 5～7 | ≥4 |
| 标准 | 7×7 | 18～20 | 3～6 | ≥7 |
| 困难 | 8×8 | 26～30 | 2～10 | ≥9 |

随机挑战仍只有 3 次失误。随机种子显示在结果页，玩家可复制种子重新挑战同一布局。

### 9.2 保证可解的逆向构造算法

不能先随机铺满再“祈祷可解”。使用逆向构造：

1. 创建空棋盘和局部随机数生成器 `rng = random.Random(seed)`；
2. 假设正在从解法最后一步向第一步放置箭头；
3. 枚举所有空格和四个方向；
4. 只保留“该方向前方没有已放置箭头”的候选；
5. 优先选择能位于更多已有箭头前方、从而形成依赖的候选；
6. 放入新箭头并记录其 ID；
7. 重复直到达到目标箭头数；
8. 将放置顺序反转，得到保证可执行的 `solution_order`；
9. 调用现有 `validate_level()` 和 `validate_solution()` 二次验证；
10. 计算初始可飞数、依赖总数和最长依赖深度，不满足当前难度阈值则换派生种子重试，最多 100 次。

正确性理由：放置某支箭头时，它前方没有已经放置的箭头；已放置箭头对应解法中更晚才移除的箭头。因此按反向后的顺序游玩时，该箭头前方的更早箭头已经移除，更晚箭头又不在它的前方，所以它一定可以飞出。

### 9.3 难度度量

生成后至少计算：

- `initial_available_count`：开局可飞箭头数，越少通常越难；
- `dependency_edges`：所有“必须先移除”的阻挡关系总数；
- `longest_dependency_depth`：最长解锁链；
- `branching_variance`：各步可选箭头数变化，用于避免全程只有唯一解而过于单调；
- 四方向数量，任一方向不能为 0。

难度标签以这些指标为准，不只看箭头数量。

### 9.4 失败回退

达到最大重试次数仍没有符合指标的布局时：

- 使用最后一个已经通过 `validate_solution()` 的可解候选；
- 将难度显示为“自定义”，不伪称困难；
- 若连可解候选都没有，则返回明确错误并让界面重新生成，不能进入空关或死局。

### 9.5 验收标准

- 同一难度和种子产生完全相同的布局与解法；
- 连续测试至少 1000 个种子，全部通过 `validate_solution()`；
- 四种方向都出现，坐标和 ID 不重复；
- 生成过程在普通电脑上 200 ms 内完成，极端情况下不阻塞主界面超过 1 秒；
- 随机生成器不修改全局 `random` 状态。

## 10. 功能 F：动画、粒子和音效

### 10.1 动画优化

在保留现有短尾迹和碰撞晃动的基础上增加：

- 成功飞出：箭头尾部产生 5～8 个同色粒子，粒子在 300 ms 内淡出；
- 碰撞：红色冲击圆环、轻微屏幕震动，幅度不超过 4 px、持续不超过 160 ms；
- 提示：金色呼吸光环，不移动箭头本体；
- 通关：星星从结果卡片中心依次弹出；
- 按钮：悬停亮度插值，不改变按钮命中矩形。

所有效果使用 `dt` 更新，禁止用 `time.sleep()`。粒子总数设置上限，例如 120 个，结束后立即移除。

### 10.2 音效设计

| 事件 | 音效特征 | 音量建议 |
|---|---|---:|
| 按钮点击 | 短、轻、清脆 | 0.25 |
| 箭头飞出 | 上扬的空气感音效 | 0.35 |
| 碰撞 | 短促、低沉，不刺耳 | 0.35 |
| 通关 | 1 秒以内的明亮和弦 | 0.45 |
| 失败 | 柔和下行音，不制造惊吓 | 0.40 |

启动时尝试 `pygame.mixer.init()`；声卡不可用或资源缺失时，将音频系统标记为不可用，游戏继续运行。音效开关保存到进度文件。

### 10.3 素材合规

- 优先使用自行制作、程序生成或明确标注 CC0 的 `.wav`；
- 在 README 中记录每个外部音效的来源链接、作者和许可证；
- 不使用商业游戏提取音效；
- 提交仓库前确认所有文件都可以随作业公开分发。

### 10.4 验收标准

- 关闭音效后所有声音立即停止，重新启动仍保持设置；
- 无音频设备时游戏不崩溃；
- 粒子和震动不影响箭头实际坐标、点击命中与规则判断；
- 60 FPS 目标下无明显卡顿，低帧率时动画速度仍基本一致。

## 11. 功能 G：打包为可执行文件

### 11.1 工具选择

使用 PyInstaller，只作为开发依赖：

```text
requirements.txt       # 仍只保留运行依赖 pygame
requirements-dev.txt   # unittest 为标准库；增加 pyinstaller
```

`requirements-dev.txt`：

```text
-r requirements.txt
pyinstaller>=6,<7
```

### 11.2 统一资源路径

打包前先将 `ASSET_ROOT` 移到 `game/resources.py`：

```python
def resource_path(*parts: str) -> Path:
    base = Path(getattr(sys, "_MEIPASS", Path(__file__).resolve().parent.parent))
    return base.joinpath(*parts)
```

所有图片、音效和图标都通过该函数读取。用户存档仍写入 `%LOCALAPPDATA%`，绝不能写入 `_MEIPASS`。

### 11.3 构建命令

Windows 初次验证可使用：

```powershell
pyinstaller --noconfirm --clean --onefile --windowed `
  --name "箭途" `
  --icon "assets/app.ico" `
  --add-data "assets;assets" `
  main.py
```

命令稳定后提交 `arrow_escape.spec`，后续统一运行：

```powershell
pyinstaller --noconfirm --clean arrow_escape.spec
```

### 11.4 发布目录

```text
release/
├─ 箭途.exe
├─ README.txt
└─ LICENSES.txt
```

发布前在一台没有配置项目虚拟环境的 Windows 电脑上测试：启动、中文显示、背景加载、音效、三关通关、存档、重启读取和退出。

### 11.5 验收标准

- 双击 `.exe` 可直接启动，不弹出命令行窗口；
- 图片、音效和字体资源不缺失；
- 安装路径包含中文或空格时仍能运行；
- 程序退出后没有残留进程；
- 可执行文件不需要用户安装 Python；
- 发布包中不包含虚拟环境、缓存、测试截图和个人存档。

## 12. 游戏界面详细改造

### 12.1 首页

保持现有标题、背景和示意卡片。按钮区调整为：

1. 有续玩存档时显示“继续游戏”，否则显示“开始游戏”；
2. “关卡选择”；
3. “玩法说明”；
4. “退出游戏”。

四个按钮总高度不能超过当前下半区；可以将按钮高度改为 48 px、间距 12 px。继续游戏为主按钮，其他为次按钮。

### 12.2 游戏页右侧面板

建议重新排列，但保持左侧棋盘位置不变：

```text
关卡名称 / 难度
━━━━━━━━━━━━━━━━
剩余箭头        12
失误机会       3/3
用时         00:24
当前分数       600

[提示 H]

[重开] [说明] [首页]
```

按钮在动画中按需禁用并显示灰色。禁用只是交互状态，布局不能跳动。

### 12.3 关卡选择页

- 每张卡片显示编号、名称、棋盘尺寸、箭头数和最佳星级；
- 悬停卡片显示青色描边；
- 锁定卡片降低亮度并绘制锁形图元；
- 一页最多 6 张卡片，暂不实现滚动；
- 底部提供三种随机难度和返回按钮。

### 12.4 结果页

在现有成功/失败符号下方增加星级、分数、时间和纪录。按钮规则：

- 固定关卡通过：“下一关”“关卡选择”“返回首页”；
- 扩展最后一关通过：“再玩一次”“关卡选择”“返回首页”；
- 随机关卡通过：“同种子重玩”“换一关”“返回首页”；
- 失败：“重新挑战”和“返回首页”。

结果弹窗出现后底层棋盘仍不可点击。

### 12.5 键盘快捷键

| 按键 | 功能 |
|---|---|
| `H` | 提示 |
| `R` | 重新开始当前关卡 |
| `Enter` | 确认当前弹窗主操作 |
| `Esc` | 关闭弹窗或返回上一层 |

鼠标仍是主要操作方式，快捷键只作为补充。

## 13. 测试计划

### 13.1 回归测试

每个阶段首先运行：

```powershell
python -m unittest discover -s tests -v
```

现有 17 项测试不得删除、跳过或降低断言强度。若数据模型新增默认字段，原测试应无需修改或只做必要的兼容调整。

### 13.2 新增自动化测试

| 编号 | 模块 | 场景 | 预期结果 |
|---|---|---|---|
| E01 | 计时 | 帮助打开时更新 10 秒 | 计时不增长 |
| E02 | 得分 | 超过标准时间 | 时间奖励为 0，不出现负奖励 |
| E03 | 星级 | 无失误、未使用提示、标准时间内 | 3 星 |
| E04 | 提示 | 三关初始状态 | 返回箭头均不受阻挡 |
| E05 | 提示 | 同状态重复请求 | 结果稳定且不改输入 |
| E06 | 存档 | 写入再读取 | 数据等价 |
| E07 | 存档 | 非法 JSON | 返回默认值且不崩溃 |
| E08 | 存档 | 非法箭头 ID | 拒绝续玩数据 |
| E09 | 关卡选择 | 锁定关卡 | 不进入游戏 |
| E10 | 生成器 | 相同种子 | 关卡完全一致 |
| E11 | 生成器 | 1000 个种子 | 全部可解且坐标合法 |
| E12 | 音频 | mixer 初始化失败 | 游戏继续运行 |
| E13 | 资源 | 模拟 `_MEIPASS` | 能解析打包资源路径 |
| E14 | 全流程 | 选择关卡→提示→通关 | 状态、分数、存档正确 |

### 13.3 手工测试

| 编号 | 操作 | 检查重点 |
|---|---|---|
| M01 | 从首页进入三个原关卡并通关 | 基础流程完全不变 |
| M02 | 关卡中打开说明停留 10 秒 | 计时暂停、文字清晰 |
| M03 | 关闭游戏再继续 | 恢复位置、失误和时间正确 |
| M04 | 完成关卡后重新挑战并取得较差成绩 | 最佳纪录不下降 |
| M05 | 生成三个难度随机关卡 | 均可游玩，困难档明显更密集 |
| M06 | 关闭系统声音设备后启动 | 游戏不崩溃 |
| M07 | 在 100%、150%、200% 缩放下运行 | 字体和图形仍清晰 |
| M08 | 在无 Python 环境运行 `.exe` | 资源、存档和退出均正常 |

### 13.4 性能目标

- 正常游戏稳定接近 60 FPS；
- 单帧逻辑和绘制通常不超过 16.7 ms；
- 提示在当前最大 32 支箭头规模下不产生可感知卡顿；
- 存档文件控制在 100 KB 以内；
- 随机关卡普通生成耗时不超过 200 ms。

## 14. 分阶段实施计划

每个阶段结束都要运行全部测试、实际打开游戏，并形成独立 Git Commit。

| 阶段 | 任务 | 主要文件 | 完成判据 | 建议 Commit |
|---|---|---|---|---|
| X0 | 冻结基础版基线，记录 17 项测试结果 | `docs/` | 基础截图和测试记录可追溯 | `docs: record extension baseline` |
| X1 | 增加计时、分数和星级纯函数 | `models.py`、`scoring.py` | 评分边界测试通过，界面暂可不显示 | `feat: add score time and star model` |
| X2 | 接入 HUD 与结果卡片 | `app.py`、`ui.py` | 游戏中和结算均显示正确 | `feat: show score time and stars` |
| X3 | 实现安全箭头搜索与提示 | `hints.py`、`app.py` | 提示永远指向可飞箭头 | `feat: add contextual hints` |
| X4 | 实现 JSON 进度与续玩 | `progress.py` | 重启后纪录和残局可恢复 | `feat: persist player progress` |
| X5 | 实现关卡选择界面 | `models.py`、`app.py`、`ui.py` | 锁定、选择和纪录显示正确 | `feat: add level selection` |
| X6 | 增加并验证第 4～6 关 | `extra_levels.py` | 每关有参考解法且可人工通关 | `feat: add challenge level pack` |
| X7 | 实现随机可解生成器 | `generator.py` | 1000 种子测试通过 | `feat: generate solvable levels` |
| X8 | 增加粒子、音效和设置 | `effects.py`、`audio.py` | 音频失败可降级，帧率正常 | `feat: polish feedback and audio` |
| X9 | 资源路径适配与 PyInstaller 打包 | `resources.py`、`.spec` | 无 Python 环境可运行 | `build: package windows executable` |
| X10 | 更新 README、博客素材和 AIGC 记录 | `README.md`、`docs/` | 文档与实际功能一致 | `docs: complete extension delivery` |

不可把 X1～X10 合并成一次巨型修改。若时间不足，应停在最近一个完整且测试通过的阶段，而不是提交多个半成品入口。

## 15. 每阶段通用完成定义

一个扩展阶段只有同时满足以下条件才算完成：

- [ ] 功能可以从正常界面进入，不依赖调试命令；
- [ ] 对基础关卡规则没有特殊分支或复制实现；
- [ ] 新增纯逻辑有自动化测试；
- [ ] 原 17 项测试全部通过；
- [ ] 开始、游戏、结果和退出流程完成一次手工测试；
- [ ] 动画期间快速连点不会重复结算；
- [ ] 错误数据或资源缺失时不会导致无说明崩溃；
- [ ] README、策划或代码注释与真实行为一致；
- [ ] Git Commit 只包含该阶段相关修改；
- [ ] 能够解释该功能的数据、算法、边界和取舍。

## 16. 风险与应对

| 风险 | 可能表现 | 处理方法 |
|---|---|---|
| 扩展状态污染基础状态机 | 通关后跳错页、重开数据残留 | 所有新会话字段统一在 `start_level()` 重置；状态切换集中处理 |
| 计时受弹窗影响 | 查看说明仍计时 | 将 `timer_running` 条件集中为一个函数并写测试 |
| 提示与规则不一致 | 高亮实际受阻箭头 | 只调用 `rules.is_blocked()`，禁止复制判断逻辑 |
| 存档损坏阻止启动 | JSON 解析异常退出 | 容错加载、版本字段、原子写入、默认进度 |
| 随机布局存在死局 | 玩家无法完成 | 逆向构造 + `validate_solution()` + 批量种子测试 |
| 新按钮挤压界面 | 文本重叠或命中区域错误 | 先确定固定布局，再分别测试 6×6、7×7、8×8 |
| 粒子影响规则坐标 | 视觉偏移导致误点 | 效果层只保存屏幕坐标，不修改 Arrow 模型 |
| 音频资源缺失 | 初始化或播放崩溃 | 音频服务统一捕获错误并退化为静音 |
| 打包后找不到素材 | 背景、音效消失 | 所有资源统一走 `resource_path()`，增加 `_MEIPASS` 测试 |
| 功能过多来不及收尾 | 多个按钮存在但不可用 | 严格按阶段交付，只保留已完成入口 |

## 17. AIGC 协作记录建议

扩展过程可以形成新的真实 AIGC 记录，建议保留：

1. 根据作业扩展要求设计模块、算法和实施顺序，即本文档；
2. 让 AI 生成随机关卡逆向构造算法初稿，再用 1000 种子测试验证和修正；
3. 让 AI 检查存档和状态机的边界条件，记录测试失败及人工修改；
4. 使用 AI 辅助生成原创音效/图标或美术素材时，保留提示词、生成日期和最终取舍；
5. 打包失败时记录报错、AI 建议、实际有效方案和最终构建命令。

每条记录必须包含“提出的问题、AI 输出摘要、运行结果、发现的问题、人工修改、对应 Commit/截图”，不能只写“使用 AI 完成代码”。

## 18. 最终扩展版验收清单

### 18.1 基础回归

- [ ] 原第 1～3 关布局和解法未改变；
- [ ] 每关仍只有 3 次失误机会；
- [ ] 四方向、边界、飞出、碰撞、失败和重开均正确；
- [ ] 原 17 项测试全部通过。

### 18.2 核心扩展

- [ ] 游戏中正确显示计时和分数；
- [ ] 通关结果显示 1～3 星且最佳纪录更新正确；
- [ ] 提示只高亮当前可飞箭头；
- [ ] 关卡选择能显示解锁与最佳纪录；
- [ ] 重新启动后能读取进度和有效续玩数据。

### 18.3 高级扩展

- [ ] 第 4～6 关均有验证过的参考解法；
- [ ] 随机关卡同种子可复现且批量验证可解；
- [ ] 音效和粒子反馈清楚且可关闭；
- [ ] 无音频设备或缺少可选资源时仍可运行；
- [ ] Windows 可执行文件在无 Python 环境可启动和保存进度。

### 18.4 交付材料

- [ ] README 包含扩展玩法、快捷键、存档位置和运行方式；
- [ ] 博客展示关卡选择、提示、随机关卡、结算评分和可执行文件；
- [ ] 新增素材来源与许可证记录完整；
- [ ] AIGC 记录真实并能对应代码、测试或截图；
- [ ] Git 历史能看出按 X0～X10 分阶段完成的过程。

---

本文档是扩展开发基线。实际实现时推荐先完成 X1～X5，形成“评分 + 提示 + 存档 + 关卡选择”的稳定扩展版，再根据剩余时间选择随机生成、音效和打包。任何扩展都必须复用现有规则层，并以不破坏基础版为最高优先级。
